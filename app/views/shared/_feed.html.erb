<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @pinned %>
    <%= render @not_pinned %>
  </ol>
  <%= will_paginate @feed_items,
                    params: { controller: :static_pages, action: :home } %>
<% end %>
