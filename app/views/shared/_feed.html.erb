<% if @feed_items.any? %>
  <ol class="microposts">
    <% if @pinned %>
      <%= render @pinned %>
    <% end %>
    <% if @not_pinned %>
      <%= render @not_pinned %>
    <% else %>
      <%= render @feed_items %>
    <% end %>
  </ol>
  <% if controller_name == 'static_pages' && action_name == 'home' %>
    <%= will_paginate @feed_items, params: { controller: :static_pages, action: :home } %>
  <% elsif controller_name == 'static_pages' && action_name == 'lately' %>
  <% end %>
<% end %>
